.container
  .d-flex.justify-content-center.h-100
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .card
        .card-header
          %h3 Conectare
        .card-body
          .input-group.form-group
            .input-group-prepend
              %span.input-group-text
                %i.fa.fa-envelope.color_white
            = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Email"
          .input-group.form-group
            .input-group-prepend
              %span.input-group-text
                %i.fa.fa-key.color_white
            = f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "Parola"
          .row.align-items-center.remember
            %input{:type => "checkbox"}>
            Tine minte parola
          .form-group.add_padding_top_5
            = f.submit "Log in", class: "btn login_btn color_white"
        .card-footer
          .d-flex.justify-content-center
            = link_to "Parola uitata?", new_user_password_path, :method => :get, class: "link color_white"
          .d-flex.justify-content-center
            = link_to "Inregistrare", new_user_registration_path, :method => :get, class: "link color_white"
